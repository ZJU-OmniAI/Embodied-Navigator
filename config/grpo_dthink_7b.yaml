# ====== GRPOConfig ======
output_dir: runs/DThinkVLN-P-7B-GRPO-r2xr-selected-6.0.0
deepspeed: config/zero2.json
per_device_train_batch_size: 2
gradient_accumulation_steps: 32
temperature: 0.7
top_p: 0.9
learning_rate: 2e-6
logging_steps: 1
bf16: true
gradient_checkpointing: true
num_train_epochs: 1
save_steps: 10
save_only_model: true
remove_unused_columns: false
report_to: wandb
run_name: DThinkVLN-P-7B-GRPO-r2xr-selected-6.0.0

num_generations: 8
max_prompt_length: 4096
max_completion_length: 512
steps_per_generation: 1
beta: 0.0
scale_rewards: group
step_advantage_coef: 1.0
traj_advantage_coef: 1.0

env_config: config/ht_dthink_base.yaml
env_type: dthink_base
rollout_max_steps: 16
step_num_samples: 4

# ====== ModelConfig ======
model_name_or_path: runs/DThinkVLN-P-7B-GRPO-r2xr-selected-5.0.0/checkpoint-110
model_revision: main
trust_remote_code: true
torch_dtype: auto
